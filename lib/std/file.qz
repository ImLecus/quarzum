import "@std/std.qz"

struct File {
    string name;
    int fd;
    bool isOpen;
}

foreign File open_file(string name, int flags, int mode);

foreign function close_file(File f);

foreign string read_file(File f);

foreign function write_file(File f, string message);

foreign string get_format(File f);

foreign function File::close();