import "@std/std.qz"

struct File {
    string name;
    int fd;
    bool isOpen;
}

foreign File openFile(string name, int flags, int mode);

foreign function closeFile(File f);

foreign string readFile(File f);

foreign function writeFile(File f, string message);

foreign string getFormat(File f);